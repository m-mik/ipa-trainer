import { Flex } from '@chakra-ui/react'

type KeyboardProps = {}

const KEYS = {
  uk: ['a', 'b'],
  us: ['c', 'd'],
}

function Keyboard(props: KeyboardProps) {
  return <Flex>keyboard</Flex>
}

export default Keyboard
